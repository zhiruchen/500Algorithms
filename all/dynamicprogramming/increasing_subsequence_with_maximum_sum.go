package dynamicprogramming

// IncreasingSubsequenceWithMaximumSum http://www.techiedelight.com/increasing-subsequence-with-maximum-sum/
func IncreasingSubsequenceWithMaximumSum() {
	//sum := make([]int32, len(seq))
	//
	//for i := 1; i < len(seq); i++ {
	//	for j := 0; j < i; j++ {
	//		if seq[j] < seq[i] && sum[i] < sum[j] {
	//			sum[i] = sum[j]
	//		}
	//	}
	//
	//	sum[i] += seq[i]
	//}

}
